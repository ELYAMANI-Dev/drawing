package com.stepbystepdrawing.HowToDrawLabubu

import android.app.Application

/**
 * Application class. OneSignal and other config-driven services are initialized
 * in SplashActivity after remote config (including apiKeys) is loaded.
 */
class DrawingStepsApplication : Application()
